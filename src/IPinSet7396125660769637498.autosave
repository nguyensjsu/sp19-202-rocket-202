interface IPinSet implements IDisplayComponent, ITouchEventHandler{
	void display();
	void setNext(IDisplayComponent next);
	void touch();
	void showError();
	void set();
	void reset();
	void update(String str);
	void cancel();
	void setClient(LoginScreen screen);
}
